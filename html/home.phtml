<!DOCTYPE html>
<!--
To change this license header, choose License Headers in Project Properties.
To change this template file, choose Tools | Templates
and open the template in the editor.
-->

<html>
    <head>
        <title>PhpFileBrowser</title>
        <meta charset="utf-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1"/>
    </head>
    
    <body>
        <box1>
            <?php foreach($itemLinks as $item => $link){ ?>
            <div>
                <a href="?route=<?php echo $link ?>">
                    <?php echo $item ?>
                </a>
            </div>
            <?php } ?>
        </box1>
        <?php if(isset($item)) { ?>
            <box3>
                <p>
                    Is het bestand schrijfbaar: <?php echo $writableText ?>.<br/>
                    Bestand groote: <?php echo $getFileSize ?>.<br/>
                    Laatst aangepast: <?php echo $getFileTime ?>
                </p>

                <?php if ($fileIsText) { ?>
                    <form method="post" id="text" name="text" action="/">
                        <textarea name="text" id="text"><?php echo $readHtmlChars ?></textarea>
                        <input class="btn btn-default btn-block" type="submit" name="save" value="Save" type="button"/> 
                    </form>
                <?php } ?>
                <?php if ($fileIsImage) { ?>
                   <img src="<?php echo $route . '/' . $file ?>" />
                <?php } ?>
            </box3>
        <?php } ?>
    </body>
</html> 